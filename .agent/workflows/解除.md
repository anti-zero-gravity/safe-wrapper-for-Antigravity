---
description: Safe Command Wrapperをアンインストールし、元のプロファイルに復元する
---
// turbo
1. 以下のコマンドを実行してSafe Command Wrapperをアンインストールする：
```powershell
& "$env:USERPROFILE\.gemini\antigravity\scratch\safe-wrapper\scripts\safe-cmd-profile-manager.ps1" -Action uninstall
```
