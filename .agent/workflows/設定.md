---
description: Safe Command Wrapperをインストールし、PowerShellプロファイルを設定する
---
// turbo
1. 以下のコマンドを実行してSafe Command Wrapperをインストールする：
```powershell
& "$env:USERPROFILE\.gemini\antigravity\scratch\safe-wrapper\scripts\safe-cmd-profile-manager.ps1" -Action install
```
